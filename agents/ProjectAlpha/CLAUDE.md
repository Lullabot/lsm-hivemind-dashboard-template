# ProjectAlpha agent instructions

This file is the agent's working memory anchor for ProjectAlpha. Replace this
text with project-specific context: code location, build commands, glossary,
key stakeholders, anything an LLM should know before touching the project.

## Memory bank

Per-project memory lives under `memory-bank/` in this directory (sibling to
this file). Maintain `activeContext.md` and `progress.md` and the dashboard
will pick up the resulting markdown.
